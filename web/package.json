{
  "name": "home-in-sight",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}